[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "evidenceflow"
version = "0.2.0"
description = "Transparent policy checks for AI-proposed claim verdicts"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
